---
import Layout from '../../layouts/Layout.astro';
import { allTags } from '../../site.config';

const title: string = 'タグ一覧';
const meta: Meta = {
  title,
  description: '色星ぴくせるのブログのタグ一覧です。',
  breadcrumb: [{ href: Astro.url.pathname, title }],
};
---

<Layout meta={meta}>
  <ul class="flex flex-wrap gap-3">
    {
      allTags.map((tag) => (
        <a href={`/tag/${tag.id}/p1/`}>
          <li class="linkItem rounded-lg bg-violet-300 px-2 py-1 shadow-md transition-shadow hover:shadow-none">
            {tag.name}
          </li>
        </a>
      ))
    }
  </ul>
</Layout>
